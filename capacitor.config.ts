import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yubipop.game',
  appName: 'Yubi Pop!',
  webDir: '.',
  server: { androidScheme: 'https' }
};

export default config;
