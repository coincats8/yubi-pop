{
  "name": "yubi-pop",
  "version": "0.1.0",
  "private": true,
  "description": "Yubi Pop! — a mobile-first thumb guessing game for Solana Mobile.",
  "scripts": {
    "sync:android": "npx cap sync android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/cli": "^7.0.0"
  }
}
